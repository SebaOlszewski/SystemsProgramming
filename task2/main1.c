#include <stdio.h>
#include <stdlib.h>

int main(int args, char * argv[]) {
	

    for (int i = 0; i < N; i++)


    printf("Smaller number: ");
    if (A > B)
    {
        printf("%d", B);
    }else{
        printf("%d", A);
    }
    printf("\n");
}